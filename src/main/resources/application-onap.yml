apiVersion: 1.0.0
logging:
    level:
        com:
            assignment:
                springboot:
                    mongo: DEBUG
    pattern:
        console: '%d{yyyy-MM-dd HH:mm:ss} - %msg%n'
server:
    port: 9006
spring:
    data:
        mongodb:
            database: BMP_JEOPARDYMANAGEMENT_1
            host: 10.0.5.36
            port: 27017
